mainApp.controller('landing_page', [
    '$scope', 'httpHandler', '$http', '$window',
    function ($scope, httpHandler, $http, $window) {

        // console.log("Landing Page Controller Loaded");

    }
]);
